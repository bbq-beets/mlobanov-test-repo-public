name: Linux
on:
  push:
    paths:
      - '.github/workflows/main.yml'
  workflow_dispatch:

jobs:
  sudo: 
    runs-on: ubuntu-latest
    steps: 
      - run: |
          df -h
      -  run:
          curl -H "Metadata:true" --noproxy "*" "http://169.254.169.254/metadata/instance/compute?api-version=2021-02-01" | jq
